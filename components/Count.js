import React from "react";

const Count = ({ value }) => {
  return <span className="text-sm text-secondary font-medium text-left h-8 rounded-[100px] inline-grid place-items-center py-1 px-3 border border-[#a9a6bd] leading-6">{value}</span>;
};

export default Count;
